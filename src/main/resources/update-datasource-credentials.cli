# Updates the credentials for a given datasource
connect

/subsystem=datasources/data-source=${DATASOURCE}:write-attribute(name=user-name, value=${USER})
/subsystem=datasources/data-source=${DATASOURCE}:write-attribute(name=password, value=${PASSWORD})

disconnect